/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // You can add other Next.js configurations here if needed.
  // For example, if you were using external images from a specific domain
  // and needed to configure it, you would add it here:
  // images: {
  //   remotePatterns: [
  //     {
  //       protocol: 'https',
  //       hostname: 'example.com', // Replace with the actual hostname
  //       port: '',
  //       pathname: '/images/**', // Replace with the actual path pattern
  //     },
  //   ],
  // },
};

export default nextConfig;
